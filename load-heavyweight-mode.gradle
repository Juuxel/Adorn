import groovy.json.JsonSlurper

def file = file("local.json")

if (!file.exists()) {
    ext.heavyweight = false
} else {
    def slurper = new JsonSlurper()
    def parsed = slurper.parse(file)
    
    ext.heavyweight = parsed.heavyweight
}
