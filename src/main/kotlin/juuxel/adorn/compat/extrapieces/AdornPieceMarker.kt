package juuxel.adorn.compat.extrapieces

/**
 * A simple marker interface for Adorn piece blocks.
 * Unlike `AdornPieceBlock`, does *not* depend on EP so can be referenced safely.
 */
interface AdornPieceMarker
