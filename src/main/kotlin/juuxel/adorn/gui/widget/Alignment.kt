package juuxel.adorn.gui.widget

/**
 * A widget alignment. Backported from LibGui for MC 1.15.
 */
enum class Alignment {
    // Using Java enum name conventions to match the 1.15 LibGui enum
    LEFT, CENTER, RIGHT
}
